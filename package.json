{
  "name": "universal-utils-vdom-components",
  "version": "0.0.11",
  "description": "vdom components library for universal-utils' vdom library (mithril-like API)",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist;",
    "deploy": "npm run build; git add --all .; git commit -am 'new build'; npm version patch; git push origin HEAD; npm publish;",
    "watch": "babel src --out-dir dist -w;"
  },
  "author": "Matt Keas (@matthiasak)",
  "license": "MIT",
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ]
  },
  "peerDependencies": {
    "universal-utils": "1.x"
  },
  "devDependencies": {
    "babel-cli": "6.x",
    "babel-core": "^6.23.1",
    "babel-polyfill": "6.x",
    "babel-preset-es2015": "6.x",
    "babel-preset-react": "6.x",
    "babel-preset-stage-0": "6.x"
  }
}
